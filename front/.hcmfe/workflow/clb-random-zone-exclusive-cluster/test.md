# clb-random-zone-exclusive-cluster 测试清单

> 工作流阶段：test  
> 依据：本迭代 `coding.md` 与 TAPD #1069995598138577666

## 验证范围

业务下公网 CLB 购买表单的随机可用区独占集群查询、独占型配置校验，以及购买成功和单据详情返回时的负载均衡 tab 定位。覆盖新增的校验交互与配置重置行为。

## 测试环境与数据

- 前端入口：`<TEST_APP_URL>`；测试账号：`<TEST_ACCOUNT>`；业务 ID：`<BIZ_ID>`。
- 准备可购买公网 CLB 的业务与云账号、支持独占集群的地域和 CMCC/CUCC/CTCC 运营商；标签数据包含四层与七层可选项。
- 为无数据、接口失败和慢响应分支准备可控测试数据或接口拦截；不要在测试产物中写真实账号、资源 ID、Cookie 或环境域名。
- P0-07 的购买提交应使用可清理的测试数据；执行后按测试环境约定清理单据和资源。

## P0 主流程

| ID | 场景 | 操作 | 期望 | covered-by | 结果 |
| --- | --- | --- | --- | --- | --- |
| P0-01 | 查询随机可用区独占集群 | 选择业务、公网、地域、三网运营商，保持“随机可用区”，查看标签查询请求 | 请求体不含 `zones`；返回可用标签后可选择“独占型” | e2e | PASS（本地 mock） |
| P0-02 | 保留指定可用区查询 | 改选具体可用区，查看标签查询请求 | 请求体仍含所选 `zones` 数组；独占型配置可继续使用 | e2e | PASS（本地 mock） |
| P0-03 | 勾选时预选首项 | 选择独占型，依次勾选四层集群和七层标签 | 四层、七层各预选第一条标签；四层集群和 IP 默认“随机分配” | e2e | PASS（本地 mock） |
| P0-04 | 显示分项校验 | 在独占型下分别验证未启用、四层配置不完整、七层标签不完整 | 错误显示在“负载均衡规格类型”选择器下；未启用提示原有至少一项规则，四层和七层显示不同文案；未完成配置不能保存 | e2e + 手测 | PASS（文案/位置 E2E + 保存门禁用户自测 2026-09-24） |
| P0-05 | 取消勾选后刷新校验 | 制造四层或七层配置错误，再取消对应勾选 | 不再显示该集群专属错误；若另一项配置完整可继续，若两项都未启用则显示至少启用一项提示 | e2e + 手测 | PASS（七层清错 E2E + 其余分支用户自测 2026-09-24） |
| P0-06 | 切换条件时清理配置 | 勾选四层与七层并选择标签，再切换账号、地域、可用区、运营商或切离独占型后返回 | 标签、集群、IP 及两个复选框重置；旧请求结果不回填新条件 | e2e + 手测 | PASS（切离独占型 E2E + 切换账号/地域/可用区/运营商用户自测 2026-09-24） |
| P0-07 | 定位负载均衡单据列表 | 从业务购买页提交一条 CLB 申请；进入 CLB 单据详情后再返回 | 购买成功和详情返回均进入业务单据列表的负载均衡 tab；URL 包含 `bizs=<BIZ_ID>&type=load-balancer` | 手测 | PASS（用户自测 2026-09-24） |

## P1 异常与回归

| ID | 场景 | 操作 | 期望 | covered-by | 结果 |
| --- | --- | --- | --- | --- | --- |
| P1-01 | 标签查询无数据或失败 | 分别返回空列表和请求失败 | 独占型不可选，沿用现有无数据或失败处理 | e2e | PASS（空列表 + HTTP 500 失败注入均 E2E 覆盖 2026-09-24） |
| P1-02 | 切换时忽略旧响应 | 使前一次标签查询延迟，再快速切换地域或可用区 | 只呈现最新条件的数据；复选框与配置不复活 | e2e | PASS（mock 扣住首次请求 1.5s 后返回 stale，切换可用区后旧数据不回填 2026-09-24） |
| P1-03 | 校验异步 IP 结果 | 选定具体四层集群，分别返回可用、无空闲 IP、请求失败 | 加载中不会误报配置错误；加载结束后校验与提示对应真实结果 | e2e | PASS（三态覆盖：延迟 1.2s 成功可选 / 空列表提示并清空 / HTTP 500 提示获取失败 2026-09-24） |
| P1-04 | 编辑已有独占型配置 | 先保存一条四层和七层配置，再打开编辑 | 已保存的勾选和选择值保持回填；不被首项默认值覆盖 | 手测 | PASS（用户自测 2026-09-24） |
| P1-05 | 校验已启用项完整性 | 同时启用四层与七层，仅完整配置其中一项并尝试保存 | 不完整的一项阻止保存，维持原有“已启用项均须完整”规则 | 手测 | PASS（用户自测 2026-09-24） |

## P2 界面核对

- [ ] 错误提示使用 BK UI FormItem 样式，位于规格类型选择器下，不在独占集群行尾重复出现。
- [ ] 勾选、取消勾选、切换标签时选择器值与禁用状态同步，无旧值残留。

## 已执行的静态检查

- 本地 ESLint 与 `git diff --check`：PASS。
- 工作流 coding 产物保鲜检查：fresh。
- 全量 `tsc --noEmit`：Blocked；未改动的 `@blueking/chat-x` 声明文件报 TS1139 等语法错误，无法作为本轮类型检查结论。

## 验证结论

本地 mock 自动化：2026-09-24 运行当前工作流的 `e2e.spec.ts`，12/12 PASS；报告位于 `e2e/playwright-report/`。首轮 7/7（P0-01～P0-06、P1-01 空列表）通过；随后扩展 mock 故障注入（tags/list 与 idle_vips/list 支持 HTTP 500、响应延迟、首次请求返回 stale 数据）补齐 P1-01 失败分支、P1-02 竞态、P1-03 三态，重跑 12/12 全部通过。P1-03 延迟用例初版在 loading 窗口内点击 IP 下拉（bk-select loading 态不展开），改为 `waitForResponse` 等响应返回后再点开后通过。临时探针已移除。

用户自测：2026-09-24 反馈 P0-04～P0-07、P1-04、P1-05 全部通过（其中 P0-04/05/06 与 E2E 覆盖互补）。

最终状态：P0 7/7、P1 5/5 全部 PASS（E2E mock + 用户手测互补覆盖）；仅 P2 界面核对未单独执行。当初标记“需网络环境”的 3 项已确认可通过 mock 故障注入在本地 E2E 模拟，无需真实测试环境。
